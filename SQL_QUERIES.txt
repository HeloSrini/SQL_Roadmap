SQL COMMANDS --- DDL,DML,DCL,DQL,TCL

DDL

CREATE -- CREATE TABLE TABLENAME (
	COL1 DATATYPE,
	COL2 DATATYPE,
	.
	.
	.
	CON N DATATYPE
	);

ADD -- ALTER TABLE TABLENAME ADD COLUMN COLUMN_NAME DATATYPE;
MODIFY -- ALTER TABLE TABLENAME MODIFY COLUMN_NAME DATATYPE;
DROP -- ALTER TABLE TABLENAME DROP COLUMN_NAME;
               DROP TABLE TABLENAME;
TRUNCATE -- TRUNCATE TABLE NTABLENAME;
RENAME -- ALTER TABLENAME RENAME OLDNAME TO NEWNAME;

DML

INSERT -- INSERT INTO TABLENAME (COLUMNS) VALUES (VAL1,VAL2,...);
UPDATE -- UPDATE TABLENAME SET COL1=VAL1,COL2=VAL2;
DELETE -- DELETE FROM TABLENAME;
	DELETE FROM TABLENAME WHERE CONDITION;
	DELETE * FROM TABLENAME;

DCL

GRANT -- 

